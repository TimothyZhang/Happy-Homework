# .secrets.env  — 复制为 .secrets.env 后填入真实值。**.secrets.env 不进 git。**
#
# 历史泄露事故:commit 580931f (2026-04-18) 误把 .secrets.env 入了仓库 +
# 推到了公开的 GitHub。任何拿到仓库的人都能 `git show 580931f:.secrets.env`
# 读出当时的值。每次新加凭证前先确认 `.gitignore` 里有 `.secrets.env`。
#
# 已发生的泄露处理步骤:
#   1) 去对应平台(本文件目前是飞书)的开发者后台 rotate secret
#   2) 用 git filter-repo --path .secrets.env --invert-paths 清掉历史,
#      force-push 前先和团队对齐(公开仓库 force-push 会让 fork 出现混乱)
#   3) 检查平台审计日志,确认泄露期内无可疑调用

# 飞书 / 多维表格调用凭证。仅 skills/lark-calendar 这条线用,小程序运行
# 时不读这两个值。
FEISHU_APP_ID=
FEISHU_APP_SECRET=
