# V1 数值设计 — 金币 / 衰减 / 商店 / 升级

> 这份笔记是把 V1 里 “奖励 → 宠物消耗 → 升级” 这条闭环显式写下来的备忘。任何一处被改动，都要回到这里把账重算一遍。

## 1. 假设：用户每天大概是怎么玩的

- 每天作业 10–20 项，全部做完 ≈ 70% 概率（剩下 30% 漏一两项）
- 用户主要在 **晚上一两个小时** 集中刷作业（pet 衰减集中在白天 + 通宵的 24h）
- 商店道具的设计目标：**早晨“补一发”** 把宠物各属性拉回安全区，不要求一天买十几次

## 2. 金币奖励

**单项作业**按作业的「应做日期」相对今天的关系分三档：

| 完成的作业类型 | 金币 | 说明 |
| --- | --- | --- |
| 补做历史遗留作业（作业日期 < 今天） | +5 | 鼓励清欠，但不如按时完成值钱 |
| 完成当日作业（作业日期 == 今天） | +10 | 基准档，沿用旧值 |
| 提前完成明日及以后的作业（作业日期 > 今天） | +15 | 比当日多 +5，奖励主动学习 |

**每天上限 20 项**：每个自然日只有前 20 项 `finishTask` 会获得金币（三种类型一起计），第 21 项起仍然能标记完成，但既不计入单项奖励、也不计入「当日全完成」基础加成里的 task count。计数器键于「完成动作发生的自然日」，不是作业本身的应做日期——所以 23:00 完成昨天的作业也算入今天的额度。

> 来源：用户硬性约束（防刷）。

其他奖励：

| 触发 | 金币 | 备注 |
| --- | --- | --- |
| 当日全部完成（首次） | + 该日已发单项金币的总和 + 早完成奖励 | 等价于把当日的单项收入再发一遍；超过 20 上限的那些 task 单项已经是 0，自然不贡献 |
| 连续 7 天全完成 | +50 | 不参与早完成奖励，不受 20 上限影响 |
| 分享被保存 | +3 / 次 | 在 `cloudfunctions/shareReward` 里硬编码，本轮不动 |

**早完成额外奖励**（叠加在「当日全部完成」加成上，单项 +5/+10/+15 和连续 +50 不受影响）：

| 完成时段（最后一项 `completedAt` 的小时，按本地时间） | 额外金币 |
| --- | --- |
| 当晚 19:00 前完成（含下午、白天、深夜跨日） | +50 |
| 19:00 – 20:00 | +30 |
| 20:00 – 21:00 | +20 |
| 21:00 之后 | +0 |

定额定义在 `store.js#EARLY_BIRD_TIERS`，纯函数 `earlyBirdBonus(date)` 给定时间返回额外金币数。`finishTask` 在最后一项完成时把 `sum(该 day 所有 occurrence.rewardPaid) + earlyBirdBonus(now)` 写入 `bonusByDay[day].dailyBonus`，`revertTask` 据此原样回滚。

单项奖励的「该次发了多少金币」写在每个 occurrence 的 `rewardPaid` 字段上（5/10/15/0），daily-perfect 基础奖励就是把这些值再加一遍。`revertTask` 按 rewardPaid 精确退款；同时把 `completionsByDay[完成日期]` 计数减 1，让被退掉的那一项把额度让回去。`rewardKind` 同时记录档位（`overdue`/`today`/`future`/`capped`），用于 UI 弹窗 caption。

> **设计 why**：
> - **三档单项奖励**：早完成（+15）激励主动学习；补做（+5）仍给一点正反馈避免「积欠到一定程度干脆放弃」，但奖励比按时低，避免「拖到周末一次性补做」反而比按时完成赚得多。
> - **20 项上限**：单一用户一天合理学习量在 10–20 项；建几十项任务再批量完成显然是刷金币行为。上限 20 也覆盖最长合理学习日。
> - **daily-perfect = 单项之和**：让「全完成奖励」与「单项奖励」始终是 ×2 关系，不管这一天里多少补做、多少提前。超过 20 上限的那些 task 单项已是 0，自动不贡献到 daily-perfect。无需在 daily-perfect 里另外算上限。
> - **早完成奖励仍是固定金币**：与任务数解耦，3 项作业的早完成不会比 12 项作业差太多。

理论日均收入（70% 完美日，N 平均 12，全部 21 点后完成，无补做也无提前）：
- 完美日（无早完成）：12 × 10 (单项) + 12 × 10 (全完成 = 同样 120) + 0 = 240
- 漏一项的日子：11 × 10 = 110
- 平均：0.7 × 240 + 0.3 × 110 ≈ **201/天**

收入弹性：
- 早完成档（21 点前完成全部）：+20/+30/+50
- 提前 N 项明日作业：+5N（单项 +5N；如果当日也都全做完，全完成里又 +5N，合计 +10N）。被 20 上限耗尽后 0。
- 补做 M 项历史作业：−5M（相对当日）；如果当日和昨日都全做完则两边各发一次单项之和。
- 例：8 项当日 + 2 项提前（明日的全部）+ 1 项补做（昨日的全部），22:00 全完成
  - 单项：8×10 + 2×15 + 1×5 = 115
  - 全完成 ×3：今日 80、明日 30、昨日 5 = 115
  - 早完成：0
  - 合计：230

保守日均仍在 200 附近，对应 §4–§6 的商店消费 / 升级节奏不变。

## 3. 宠物衰减（`PET_DECAY_PER_HOUR`）

目标：每个属性 100 → 50 大约 12–20 小时之间。

| 属性 | 每小时衰减 | 100→50 | 100→0 |
| --- | --- | --- | --- |
| 饱腹 fullness | 4   | 12.5h | 25h |
| 清洁 cleanliness | 3   | 16.7h | 33h |
| 开心 happiness | 3   | 16.7h | 33h |
| 健康 health | 2.5 | 20h   | 40h |

**Why 这样分**：饱腹掉得最快（最频繁需要照顾，对应最常见的胡萝卜/便当），健康掉得最慢（长线属性，几天买一次维生素就行）。开心和清洁居中。所有属性都会在 12-20h 内进入“需要补”的范围，没有“摆设属性”。

完成单项作业**不再附带宠物属性奖励**（旧版会 +6 happiness）。开心值只能通过商店购买/早完成奖励 → 多金币 → 商店补给来恢复，让金币奖励真正成为唯一回路。

## 4. 商店道具

每个属性都有 **便宜 + 中档** 两档可选；蝴蝶结作为唯一的「高档」装饰品担任奖励性消费。
价格区间：便宜 15-25、中档 25-40、高档 50-80。一次补给覆盖 12-24 小时。

| id | emoji | 名字 | 效果 | 价格 | 档位 | 主补 | 次补 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 🥕 | 营养胡萝卜 | 饱腹+30 开心+4 | 16 | 便宜 | fullness | happiness |
| 2 | 🍱 | 丰盛便当 | 饱腹+50 开心+8 | 28 | 中档 | fullness | happiness |
| 3 | 🧼 | 香皂 | 清洁+30 | 18 | 便宜 | cleanliness | — |
| 4 | 🛁 | 泡泡浴 | 清洁+60 开心+5 | 32 | 中档 | cleanliness | happiness |
| 5 | 🧸 | 陪玩玩具熊 | 开心+25 | 18 | 便宜 | happiness | — |
| 6 | 💊 | 维生素 | 健康+25 | 20 | 便宜 | health | — |
| 7 | 🏃 | 健身房一次 | 健康+55 开心+5 | 35 | 中档 | health | happiness |
| 8 | 🎀 | 粉色蝴蝶结 | 开心+15 形象更可爱 | 50 | 高档 | happiness | （装饰） |

> 注：旧版本里每个道具有 `growth` 字段，用来推升级条。这版升级改为“消耗金币”，所以 `growth` 字段一并删掉。
> 注 2：`shopItems` 是配置而非用户状态，`migrateState` 在每次启动时都会从 `defaultState` 刷新一次，确保改完道具后老用户也能拿到新表，不需要清缓存。

### 一日典型消费（参考）
- 饱腹早晚补一次：胡萝卜 16 + 便当 28 ≈ **44**（或两次胡萝卜 32）
- 清洁一次：香皂 + 泡泡浴 交替，平均 ≈ **22**（中档 32 / 1.5 天 ≈ 21；偶尔 18 元的香皂兜底）
- 开心兜底（任务少时）：玩具熊 18 × 平均 0.4 次 ≈ **7**
- 健康（每两到三天一次）：维生素 20 × 0.4 + 健身房 35 × 0.15 ≈ **13**
- 蝴蝶结奖励性消费：50 × 0.05 次/天 ≈ **3**

合计约 **85–95 / 天**，落在用户给的 60-100 区间。新加的清洁、健康中档道具吃掉一点缓冲，但因为单次回复更高、买的频次更低，**总日消费没有突破上限**。

> 维护提示：如果未来再加道具导致超出区间，**优先降低道具频次假设或微调原有道具价格**（如把蝴蝶结频次按 0.03 次/天计算），不要给新道具大幅加价——便宜档应保持在 15-25 区间。

## 5. 升级曲线（消耗金币换等级）

| 升级 | 所需金币 | 设计天数（日均 70 攒） |
| --- | --- | --- |
| L1 → L2 | 180 | ≈ 2.5 天 |
| L2 → L3 | 500 | ≈ 7 天 |
| L3 → L4 | 1000 | ≈ 14 天 |
| L4 → L5 | 2000 | ≈ 28 天 |
| L5 → L6 及以后 | 2000 / 级 | 1 月 / 级 |

**Why 是消耗式而不是 XP**：
1. UI 简单——一个进度条 + 一个按钮就完事，不需要新的 XP 字段同步
2. 让金币真正“值钱”——用户必须在“喂宠物 vs 攒升级”之间做权衡
3. 升级动作有仪式感——用户主动按按钮“升”，比沉默地经验涨满更带感

进度条显示 `min(coins, levelCost) / levelCost`，凑够后“升级”按钮亮起，点击扣金币 + 等级 +1。

**视觉解锁**（极简）：等级显示从 `Lv.X` 开始，到 L3 加 ⭐，L5 加 👑。SVG 不变。

## 6. 闭环验算（日均）

```
日均收入  ≈ 200
日均消耗 (商店) ≈ 80
剩余    ≈ 120  →  攒升级用
```

按攒升级 120/天 节奏：
- L1→L2：180 / 120 = 1.5 天 ✓ （目标 2-3 天，偏快但首升级偏快是好事）
- L2→L3：500 / 120 = 4.2 天 ✓ （目标 1 周，比预期快——若用户多花在道具上会刚好）
- L3→L4：1000 / 120 = 8.3 天 ✓
- L4+：2000 / 120 = 16.7 天（目标 30 天）

**这里跑偏**：日均 200 时升级太快。但用户给的“日均 100-200”含混，且实际用户不一定每天 12 项任务、不一定每天完美。**我假设保守玩家场景下日均 70 用于升级**（很多天漏题、买了非必需的蝴蝶结、临时塞红包），这样 L4→L5 大约 30 天，符合目标。

如果实际跑下来发现“升级太快/太慢”，调节优先顺序：
1. 先调 `LEVEL_COSTS` 系数（成本是最直接的旋钮）
2. 不要去动 `+10/任务` 的核心规则（用户硬性约束）
3. 如果想让中后期更慢，调高 L4+ 单级成本到 3000

## 7. 校验脚本

`scripts/values-check.js` 做这些断言：
- 完成今日单任务 → coins +10；补做昨日任务 → +5；提前完成明日任务 → +15
- 每日 20 项上限：连续完成 25 项今日任务，第 21–25 项 `rewardPaid=0`，coins 增量只有 20×10
- revert 一项已发金币的任务 → coins 减回去 5/10/15，且 `completionsByDay[完成日]` 计数 −1；revert 一项 capped 的任务 → coins 不变
- 当天 8 项全做完（22:00 时段，无早完成奖励）→ 累计 = 8 × 10 (单项) + 8 × 10 (全完成 = 单项之和) = 160
- daily-perfect 等于单项之和：当天 25 项全做完（22:00）→ 单项之和 200（20×10，5 项 capped），全完成 +200，合计 400
- daily-perfect 跟随档位：3 项当日 + 2 项提前（明日的全部），全在今天 22:00 完成 → 当日 perfect 3×10+3×10=60；明日 perfect 2×15+2×15=60；合计 + 单项 60 = 180（其中明日的 perfect 提前发了，因为明日的 2 项已全做完）
- 早完成奖励：`earlyBirdBonus` 在 05:00/14:00/18:59/19:00/19:59/20:00/20:30/21:00/23:30 各时刻返回 50 / 50 / 50 / 30 / 30 / 20 / 20 / 0 / 0
- 同样 8 项今日任务，在 18:30 完成 = 210；19:00 = 190；20:30 = 180；22:00 = 160
- 升级表：getLevelCost(1)=180, =2)=500, =3)=1000, =4)=2000, =5)=2000
- 衰减：fullness 16h 后从 100 → 36（近似）；health 16h 后从 100 → 60
- levelUpPet：金币不够时不变，金币够时 -180 / level +1

> 维护提示：脚本在文件顶部把 `Date.now` 钉到 22:00（无早完成档），后面的奖励断言才不会随真实运行时刻飘。早完成档断言通过 `setNowHour(h)` 临时切换，跑完后切回 22:00。
