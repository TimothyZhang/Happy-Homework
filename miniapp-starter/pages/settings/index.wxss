.set-card { margin-bottom: 24rpx; }
.set-head { font-size: 28rpx; font-weight: 700; color: #44505e; }

.lang-toggle { margin-top: 18rpx; display: flex; gap: 16rpx; }
.lang-opt {
  flex: 1; height: 88rpx; border-radius: 18rpx;
  display: flex; align-items: center; justify-content: center;
  background: #f4f6fa; color: #1f2329; font-size: 30rpx; font-weight: 700;
  box-shadow: inset 0 0 0 2rpx #eef1f5;
}
.lang-opt.on { background: #245bdb; color: #ffffff; box-shadow: none; }
.lang-opt-hover { background: #e6ebf3; }

.set-entry { display: flex; align-items: center; margin-bottom: 24rpx; }
.set-entry-hover { background: #f5f8ff; }
.set-entry-text { flex: 1; display: flex; flex-direction: column; min-width: 0; }
.set-entry-title { font-size: 30rpx; font-weight: 700; color: #1f2329; }
.set-entry-sub { margin-top: 6rpx; font-size: 23rpx; color: #8a94a0; }
.set-entry-chev { color: #c7cdd6; font-size: 36rpx; flex-shrink: 0; }
