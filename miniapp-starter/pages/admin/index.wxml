<view class="page">
  <view class="card">
    <view class="section-header">
      <text class="section-title">{{t.admin_userList}}</text>
      <text class="subtle">{{totalLabel}}</text>
    </view>
    <input class="input search-input"
           placeholder="{{t.admin_searchPlaceholder}}"
           value="{{keyword}}"
           bindinput="handleKeywordInput" />
    <view class="fb-entry" bindtap="goFeedback" hover-class="fb-entry-hover">
      <text>💬 {{t.afb_entry}}</text>
      <text class="fb-entry-chev">›</text>
    </view>
  </view>

  <view wx:if="{{loading}}" class="empty-block">{{t.admin_loading}}</view>

  <view wx:elif="{{error}}" class="card error-card">
    <text class="error-text">{{error}}</text>
  </view>

  <view wx:elif="{{filteredUsers.length === 0}}" class="empty-block">
    <text>{{t.admin_noMatch}}</text>
  </view>

  <view wx:else class="user-list">
    <view wx:for="{{filteredUsers}}"
          wx:key="openid"
          class="card user-row"
          data-openid="{{item.openid}}"
          bindtap="handleOpenDetail">
      <view class="user-avatar">
        <image wx:if="{{item.avatar}}" class="avatar-img" src="{{item.avatar}}" mode="aspectFill" />
        <view wx:else class="avatar-fallback">{{item.avatarInitial}}</view>
      </view>

      <view class="user-info">
        <view class="user-line top">
          <text class="user-name">{{item.displayName}}</text>
          <text class="coin-pill">🪙 {{item.coins}}</text>
        </view>

        <view class="user-line">
          <text class="openid-text">{{item.shortOpenid}}</text>
          <text class="subtle">{{item.updatedAtDisplay}}</text>
        </view>

        <view class="user-stats">
          <text class="stat-pill">🔥 {{item.streakDays}} {{t.admin_streakSuffix}}</text>
          <text wx:if="{{item.pet}}" class="stat-pill">
            {{item.pet.emoji}} {{item.petDisplayName}} Lv.{{item.pet.level}}
          </text>
          <text wx:else class="stat-pill subtle">{{t.admin_noPet}}</text>
          <text class="stat-pill">📚 {{item.notebookCount}} {{t.admin_notebookSuffix}}</text>
          <text class="stat-pill">📝 {{item.taskCount}} {{t.admin_taskSuffix}}</text>
        </view>
      </view>
    </view>
  </view>
</view>
