.page {
  padding-bottom: calc(24rpx + 100rpx + env(safe-area-inset-bottom));
}

/* === Profile card === */

.profile-card {
  margin-bottom: 24rpx;
}

.profile-row {
  margin-top: 22rpx;
  display: flex;
  align-items: center;
  gap: 28rpx;
}

.avatar-wrap {
  position: relative;
  width: 144rpx;
  height: 144rpx;
  border-radius: 50%;
  background: #eef4ff;
  overflow: hidden;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4rpx 14rpx rgba(36, 91, 219, 0.12);
}
.avatar-wrap.is-loading { opacity: 0.7; }
.avatar-img {
  width: 100%;
  height: 100%;
  display: block;
}
.avatar-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #245bdb;
}
.avatar-placeholder-icon {
  font-size: 56rpx;
  font-weight: 300;
  line-height: 1;
}
.avatar-placeholder-hint {
  margin-top: 6rpx;
  font-size: 22rpx;
  color: #6b7785;
}
.avatar-loading-mask {
  position: absolute;
  inset: 0;
  background: rgba(31, 35, 41, 0.42);
  color: #ffffff;
  font-size: 22rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.profile-fields {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}
.profile-fields .field-label {
  margin-bottom: 4rpx;
}
.nickname-input {
  margin-top: 4rpx;
}
.profile-hint {
  margin-top: 10rpx;
  font-size: 22rpx;
  color: #8a94a4;
}

/* 只读态昵称(非编辑模式直接显示文字) */
.nickname-text {
  margin-top: 6rpx;
  font-size: 32rpx;
  font-weight: 600;
  color: #1f2329;
  line-height: 1.3;
}

/* 「编辑 / 完成」开关(学生信息卡右上角) */
.edit-toggle {
  font-size: 26rpx;
  font-weight: 600;
  color: #245bdb;
  background: #eef4ff;
  padding: 8rpx 24rpx;
  border-radius: 999rpx;
  line-height: 1;
}
.edit-toggle.done {
  color: #ffffff;
  background: linear-gradient(135deg, #245bdb 0%, #1546b5 100%);
}

/* 编辑态:头像加蓝色高亮环 + 底部「换」带,提示可点换图 */
.avatar-wrap.editable {
  box-shadow: 0 0 0 4rpx rgba(36, 91, 219, 0.35);
}
.avatar-edit-badge {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  padding: 8rpx 0;
  background: rgba(36, 91, 219, 0.85);
  color: #ffffff;
  font-size: 20rpx;
  font-weight: 600;
  text-align: center;
  line-height: 1;
}

/* === Sync card === */

.sync-pill {
  padding: 6rpx 18rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
}
.sync-pill.healthy { background: #e8f7ee; color: #067647; }
.sync-pill.readonly { background: #fff5e6; color: #b26a00; }
.sync-pill.error { background: #fff1f0; color: #e54545; }
.sync-pill.unknown { background: #edf0f3; color: #5f6b7a; }

.sync-meta {
  margin-top: 18rpx;
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}
.sync-meta-line {
  font-size: 24rpx;
  color: #6b7785;
  line-height: 1.5;
  word-break: break-all;
}
.sync-meta-line.warn { color: #b26a00; }
.sync-meta-line.err { color: #e54545; }

.sync-actions {
  margin-top: 22rpx;
  display: flex;
  gap: 16rpx;
}

.sync-btn {
  flex: 1;
  height: 76rpx;
  padding: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 600;
  border: none;
  margin: 0;
  line-height: 1;
}
.sync-btn.primary {
  background: linear-gradient(135deg, #245bdb 0%, #1546b5 100%);
  color: #ffffff;
  box-shadow: 0 6rpx 18rpx rgba(36, 91, 219, 0.22);
}
.sync-btn.ghost {
  background: #eef4ff;
  color: #245bdb;
}
.sync-btn[disabled] { opacity: 0.6; }

/* === Organization tags(「我」tab 只读概览;增删改在 pages/org-tags) === */

.org-summary-card {
  margin-top: 24rpx;
}
.org-summary-right {
  display: flex;
  align-items: center;
  gap: 6rpx;
}
.org-summary-count {
  font-size: 22rpx;
  font-weight: 600;
  color: #6b7785;
  background: #eef0f3;
  padding: 4rpx 14rpx;
  border-radius: 999rpx;
}
.org-summary-chev {
  font-size: 36rpx;
  color: #c1c8d2;
  line-height: 1;
}
.org-chips {
  margin-top: 18rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
}
.org-chip {
  font-size: 24rpx;
  color: #245bdb;
  background: #eef4ff;
  padding: 10rpx 22rpx;
  border-radius: 999rpx;
  line-height: 1;
}
.org-summary-empty {
  display: block;
  margin-top: 18rpx;
  font-size: 24rpx;
  color: #8a94a4;
}
.org-summary-hint {
  display: block;
  margin-top: 16rpx;
  font-size: 22rpx;
  color: #aab3bd;
}

/* === Admin entry === */

.admin-card {
  margin-top: 24rpx;
}
.admin-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18rpx;
}
.admin-text {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  flex: 1;
}
.admin-title {
  font-size: 30rpx;
  font-weight: 700;
  color: #1f2329;
}
.admin-hint {
  font-size: 24rpx;
  color: #6b7785;
}
.admin-arrow {
  font-size: 40rpx;
  color: #c1c8d2;
  line-height: 1;
}

/* === Openid debug card === */

.openid-card {
  margin-top: 24rpx;
}
.openid-badge {
  padding: 6rpx 18rpx;
  border-radius: 999rpx;
  font-size: 22rpx;
  font-weight: 600;
  background: #fff5e6;
  color: #b26a00;
}
.openid-badge.admin {
  background: #e8f7ee;
  color: #067647;
}
.openid-box {
  margin-top: 18rpx;
  padding: 18rpx 20rpx;
  background: #f7f9fc;
  border-radius: 16rpx;
  display: flex;
  align-items: center;
  gap: 16rpx;
}
.openid-value {
  flex: 1;
  font-family: 'SF Mono', Menlo, Consolas, monospace;
  font-size: 24rpx;
  color: #1f2329;
  word-break: break-all;
  line-height: 1.5;
}
.openid-value.selectable {
  user-select: text;
}
.openid-copy {
  flex-shrink: 0;
  padding: 6rpx 18rpx;
  border-radius: 999rpx;
  background: #245bdb;
  color: #fff;
  font-size: 22rpx;
  font-weight: 600;
}
.openid-hint {
  margin-top: 14rpx;
  font-size: 22rpx;
  color: #6b7785;
  line-height: 1.6;
}

.build-info {
  margin: 36rpx 24rpx 24rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4rpx;
}

.build-info-line {
  font-size: 22rpx;
  color: #8a96a6;
  font-family: -apple-system, "SF Mono", Menlo, monospace;
}

.build-info-line.dim {
  color: #b4bdc9;
  font-size: 20rpx;
}

/* 我的单词本入口卡片 */
.wb-entry-card { display: flex; align-items: center; margin-bottom: 24rpx; }
.wb-entry-hover { background: #f5f8ff; }
.wb-entry-emoji { font-size: 48rpx; margin-right: 18rpx; }
.wb-entry-text { flex: 1; display: flex; flex-direction: column; min-width: 0; }
.wb-entry-title { font-size: 30rpx; font-weight: 700; color: #1f2329; }
.wb-entry-sub { margin-top: 6rpx; font-size: 22rpx; color: #8a94a0; }
.wb-entry-chev { color: #c7cdd6; font-size: 36rpx; flex-shrink: 0; }
